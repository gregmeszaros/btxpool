{% include 'header.html' %}

<div id="main">
    <div class="intro">
        <h1 class="header-h1">Last 30 Blocks</h1>
        <div class="minercontent">

            Last 30 blocks mined by this pool.

            <div>
                <strong>Last block found: {{ last_found }} ago</strong>
            </div>

            <table class="table">
                <tr>
                    <th>Block number</th>
                    <th>Block reward</th>
                    <th>Time found</th>
                    <th>Confirmations</th>
                    <th>Block finder</th>
                </tr>

                {% for block in blocks %}
                    <tr>
                        <td> See earnings for <a href="index.php?coin={{ coin_seo_name }}&page=block-earnings&id={{ block.id }}" > {{ block.height }} </a></td>
                        <td> {{ block.amount }} {{ getCoinSettings(coin_seo_name).symbol }} </td>
                        <td> {{ showDateTime(block.time) }} </td>
                        <td> {{ formatConfirmations(block.confirmations, block.category) }} </td>
                        <td><a href="index.php?coin={{ coin_seo_name }}&page=dashboard&wallet={{ block.username }}">{{ block.username }}</a></td>
                    </tr>
                {% endfor %}

            </table>

            <div id="container-block-diff" style="min-width: 310px; height: 300px; margin: 0 auto"></div>

        </div>
    </div>
</div>

{% include 'footer.html' %}